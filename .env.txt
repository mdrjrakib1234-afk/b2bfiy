# Neon Postgres Configuration (Required for persistent data + admin login)
# Get this from the Neon Console -> your project -> Connection Details ->
# "Pooled connection" (psql format). Server-only secret — never exposed to
# the browser.
DATABASE_URL=postgresql://neondb_owner:npg_ksow1a8hKJxr@ep-icy-block-b31kbwit-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Admin Authentication (Required to sign in at /admin)
# ADMIN_JWT_SECRET: any long random string, used to sign admin session cookies.
#   Generate one with: openssl rand -hex 32
# ADMIN_EMAIL / ADMIN_PASSWORD: used ONCE to bootstrap your first admin
#   account into the `admin_users` table on your first successful login.
#   Safe to remove from your env vars after that first sign-in.
ADMIN_JWT_SECRET=4e8b39ca82d614a90cb06fbfaef78201de3cb37719601d36beed38258e74ba9a
ADMIN_EMAIL=a@g.com
ADMIN_PASSWORD=@b2bfiy@

# Server-side Analytics & Tracking (Optional - used by /api/track)
META_CAPI_ACCESS_TOKEN=
GA4_MEASUREMENT_ID=
GA4_API_SECRET=

# Gemini API Key (Server-side)
GEMINI_API_KEY=
